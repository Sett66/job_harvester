# JH-03 — 事件流与阶段派生

| 字段           | 值                                            |
| -------------- | --------------------------------------------- |
| **Type**       | AFK                                           |
| **Wave**       | W2                                            |
| **Blocked by** | [JH-02](./02-投递记录CRUD.md)                 |
| **Blocks**     | JH-04, JH-05, JH-09, JH-10                    |
| **估时**       | 1 天                                          |
| **状态**       | ✅ 已完成                                      |

---

## 1. 目标

这是整个系统的核心机制。包含三部分：

1. `event` 表 + 添加/编辑/删除事件的 API
2. **从事件派生投递状态的纯函数**（本片最重要的产出）
3. 投递详情页的事件时间线

派生函数的签名大致是：输入一条投递的全部事件（按 `occurredAt` 排序），输出 `stage`、`ball`、`outcome`、`currentRound`、`currentInterviewType`、`lastEventAt`、`nextDeadlineAt`。

---

## 2. 背景

系统的全部状态都由事件派生，`application` 表上的状态字段只是缓存。这个设计的直接来源是真实数据的形态：Excel 表头只到「一面」，E 列有数据却没有表头——固定阶段列必然被现实打破；而单元格里「当天测评，29笔试，4.7约面试时间」这样的内容说明真实记录粒度本就是事件流，只是被二维表压扁了。

`REVIVED` 不是假想场景：腾讯元宝走的是「无投递记录 → 被捞起 → 一面 → 二面 → 挂」，腾讯视频是「5.15 捞起来 → 5.20 一面」。**终止状态必须可逆。**

---

## 3. 前置阅读

1. [`docs/DESIGN.md`](../DESIGN.md) 4.2（阶段的二维建模）—— 特别是「为什么必须有第二维」
2. [`docs/DESIGN.md`](../DESIGN.md) 4.3（事件类型）
3. [`docs/DESIGN.md`](../DESIGN.md) 4.4（表结构）中的 `event`
4. [`docs/DESIGN.md`](../DESIGN.md) 第 2 节原则 1（原始数据不可变）与原则 2（能算出来的就不要存）
5. [JH-02](./02-投递记录CRUD.md) 已确立的四组枚举

---

## 4. 详细规格

### 4.1 派生规则

必须严格按此实现：

| 事件 | 派生结果 |
|---|---|
| `APPLY` | stage=APPLIED, ball=THEM |
| `SCREEN_PASS` | stage=SCREENING, ball=THEM |
| `SCREEN_FAIL` | stage=CLOSED, outcome=REJECTED, ball=null |
| `ASSESSMENT_INVITE` | stage=ASSESSMENT, ball=**ME**, nextDeadlineAt=事件的 deadlineAt |
| `ASSESSMENT_DONE` | stage=ASSESSMENT, ball=THEM, 清除 nextDeadlineAt |
| `EXAM_INVITE` | stage=WRITTEN_EXAM, ball=**ME**, nextDeadlineAt=事件的 deadlineAt |
| `EXAM_DONE` | stage=WRITTEN_EXAM, ball=THEM, 清除 nextDeadlineAt |
| `INTERVIEW_SCHEDULED` | stage=INTERVIEW, ball=**ME**, currentRound=事件的 round, currentInterviewType=事件的 interviewType, nextDeadlineAt=事件的 scheduledAt |
| `INTERVIEW_DONE` | stage=INTERVIEW, ball=THEM, 清除 nextDeadlineAt |
| `OFFER_INTENT` / `OFFER_FORMAL` | stage=OFFER, ball=null |
| `REJECTED` | stage=CLOSED, outcome=REJECTED, ball=null |
| `WITHDRAWN` | stage=CLOSED, outcome=WITHDRAWN, ball=null |
| `REVIVED` | 从 CLOSED 恢复到被终止前的环节，ball=THEM，清除 outcome |
| `NOTE` | **不改变任何状态** |

`REVIVED` 的实现需要特别注意：它要把投递从 `CLOSED` 拉回进行中，恢复到 `REJECTED` 事件之前的状态。

### 4.2 关键约束

- **派生函数必须是纯函数**，放在 `packages/shared`，前后端都能调用。不要写成依赖数据库的 service 方法
- `application` 表上的 `stage` / `ball` / `currentRound` / `lastEventAt` / `nextDeadlineAt` 都是**从事件派生的冗余缓存**，存在的唯一目的是方便排序和筛选。必须提供一个「全量重算」命令，任何时候都能从 `event` 表重建这些字段，且结果与增量更新完全一致
- `event.rawText` 保留原始文本片段。像「简历没过，应该是还在 cd」这种带主观推测的记录，原文必须留着，不允许在结构化时丢弃
- 事件可以手动添加、编辑、删除，删除后状态要自动重算
- 时间线要能显示事件类型、发生时间、来源（邮件/手动/导入）和原文

---

## 5. 建议改动文件

| 路径                                              | 动作                              |
| ------------------------------------------------- | --------------------------------- |
| `packages/shared/src/schemas/event.ts`            | 新建（事件类型枚举 + Zod）        |
| `packages/shared/src/derive/application-state.ts` | 新建（**派生纯函数**，本片核心）  |
| `packages/shared/src/index.ts`                    | 扩展（导出）                      |
| `apps/server/src/db/schema.ts`                    | 扩展（`event` 表）                |
| `apps/server/src/modules/events/`                 | 新建（module/controller/service） |
| `apps/server/src/modules/applications/`           | 扩展（写入派生缓存字段）          |
| `apps/server/src/commands/recompute.ts`           | 新建（全量重算命令）              |
| `apps/web/src/features/applications/detail/`      | 新建（详情页 + 事件时间线）       |

**不要改**：JH-02 已确立的四组枚举定义。

---

## 6. Acceptance criteria

- [x] 能给投递添加事件，投递的当前状态自动更新
- [x] 详情页显示按时间排序的事件时间线
- [x] 派生纯函数有单元测试，至少覆盖：完整正常流程、`REVIVED` 复活、`NOTE` 不改变状态、多轮面试轮次正确递增、`ASSESSMENT_INVITE` 把球放到 ME 且带截止时间
- [x] 有全量重算命令，对已有数据重算后结果与增量更新一致
- [x] 删除中间某个事件后，状态能正确重算
- [x] 用腾讯元宝的真实案例做一个测试：无投递 → REVIVED → INTERVIEW_SCHEDULED(1) → INTERVIEW_DONE → INTERVIEW_SCHEDULED(2) → REJECTED，最终状态正确

---

## 7. 验证步骤

```powershell
pnpm --filter shared build
pnpm typecheck
pnpm test    # 派生纯函数的单测必须全绿

# 全量重算一致性验证
pnpm --filter server recompute
# 预期：重算前后 application 的 stage/ball/currentRound/lastEventAt/nextDeadlineAt 完全一致

pnpm dev
# 手测清单：
# 1. 给一条投递依次加 APPLY → EXAM_INVITE(带 deadlineAt) → EXAM_DONE
#    —— ball 应从 THEM → ME → THEM，nextDeadlineAt 应出现后清除
# 2. 加 NOTE 事件 —— 状态不应有任何变化
# 3. 加 REJECTED 后再加 REVIVED —— 应从 CLOSED 回到终止前的环节
# 4. 删除中间的 EXAM_INVITE —— 状态应自动重算
```

---

## 8. 不做

- 看板视图、今日待办、沉寂检测（JH-04）——本片只提供 `ball` 与 `lastEventAt` 这两个派生结果
- 把沉寂状态落库（违反原则 2，沉寂在 JH-04 中查询时计算）
- 从邮件自动生成事件（JH-09）
- Excel 导入（JH-05）
- 把派生逻辑写成依赖数据库的 service 方法

---

## 9. 交付物 / 下游

| 交付物                                        | 消费者                                      |
| --------------------------------------------- | ------------------------------------------- |
| `event` 表 + 事件 CRUD API                    | JH-05（导入写事件）、JH-09（确认后写事件）、JH-10（面试事件关联） |
| **派生纯函数**（stage/ball/round/deadline）    | JH-04（看板分栏与待办排序）、JH-05（导入后校验） |
| 全量重算命令                                  | JH-05（导入后重算）、JH-09（重跑抽取后重算）|
| `lastEventAt` + `ball` 派生结果                | JH-04（沉寂检测的两个输入）                 |
| 事件时间线组件                                | JH-09（确认队列展示上下文）、JH-12（简报取历史事件） |
| `event.rawText` 保留原文的约定                | JH-05, JH-09, JH-10                         |
