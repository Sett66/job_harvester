# 07. 邮箱同步与原始邮件入库

**类型**：AFK
**依赖**：01 项目骨架
**先读**：`docs/DESIGN.md` 5.1（邮件接入）、第 2 节原则 1（原始数据不可变）

## 要构建什么

连接 QQ 邮箱，把邮件同步到 `email` 表，页面提供手动同步按钮和邮件列表。

**本片不做任何解析和分类**——那是第 08、09 片的事。这里只负责把邮件原封不动地搬进来。

## 关键约束（这里有两个必踩的坑）

**坑一：QQ 邮箱会拒绝未发送 `ID` 命令的客户端。** 直接登录会报 `Unsafe Login. Please contact xiaoma@qq.com for help`。登录后必须发送 `ID` 命令声明客户端信息才能正常使用。

**坑二：招聘邮件不一定在 `INBOX`。** QQ 邮箱的自动分类会把不少 HR 系统发的邮件归入「订阅邮件」文件夹，部分还会进垃圾箱。**只扫 `INBOX` 一定会漏邮件**，必须遍历多个文件夹。

其余约束：

- 服务器 `imap.qq.com:993` SSL，认证方式是**授权码**（不是 QQ 密码，也不需要 OAuth）
- 用 `imapflow` 做 IMAP 客户端、`mailparser` 解析邮件格式，二者同一作者维护
- **授权码存系统凭据管理器**（`keytar` / Windows 凭据管理器），不写 `.env`、不入库。数据库只存邮箱地址
- 回溯范围从 **2026-02-01** 起，做成配置项。（用户首条投递是 3.11，往前留一个月覆盖宣讲会和内推开放通知）
- `syncState` 表记录每个文件夹的 `lastUid`，支持增量同步
- **大 HTML 正文落盘**到 `data/mails/`，数据库只存路径；纯文本正文入库便于搜索
- 附件落盘到 `data/attachments/`，`attachment` 表存元信息
- **单封邮件解析失败不能中断整批同步**，记录错误继续处理
- 同步中断后再次执行要能续传，不从头开始
- **只做手动同步按钮，不做定时任务**。定时同步是第 13 片。开发期需要对同一批邮件反复重跑，后台任务会干扰调试
- **不做 IMAP IDLE**。招聘邮件晚十五分钟看到不影响任何决策，不值得维护长连接重连逻辑

## 验收标准

- [ ] 点击同步按钮能拉取邮件并入库
- [ ] `ID` 命令已正确发送，登录不报 Unsafe Login
- [ ] 「订阅邮件」等多个文件夹都被扫描到
- [ ] 重复同步不产生重复记录（`messageId` 唯一）
- [ ] 邮件列表页可查看发件人、主题、时间、正文
- [ ] 大 HTML 正文已落盘，数据库中只有路径
- [ ] 附件已下载到 `data/attachments/`
- [ ] 单封邮件失败时同步继续，错误被记录
- [ ] 中断后再同步能从断点继续
- [ ] 授权码不出现在代码、配置文件和数据库中

## 阻塞关系

被 01 阻塞。与第 02、06、11 片可并行。完成后解锁第 08 和第 13 片。
